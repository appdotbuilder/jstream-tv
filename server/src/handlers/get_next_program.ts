import { type EpgProgram } from '../schema';

export const getNextProgram = async (channelId: number): Promise<EpgProgram | null> => {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is fetching the next upcoming program for a specific channel.
    // Should find the program that starts after the current time, ordered by start_time.
    return null;
};