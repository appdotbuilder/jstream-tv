import { type EpgProgram } from '../schema';

export const getCurrentProgram = async (channelId: number): Promise<EpgProgram | null> => {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is fetching the currently airing program for a specific channel.
    // Should find the program where current time is between start_time and end_time.
    return null;
};